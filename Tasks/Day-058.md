# Day 58 – Deploy Grafana on Kubernetes Cluster

---

# Task Overview

The Nautilus DevOps team planned to deploy **Grafana** on the Kubernetes cluster to visualize and analyze application metrics and infrastructure monitoring data.

Grafana is one of the most widely used observability tools in modern DevOps environments. It helps teams create dashboards, monitor systems, and analyze performance in real time.

The objective was to:

* Create a Deployment named `grafana-deployment-datacenter`
* Deploy Grafana using a Grafana image
* Create a NodePort Service
* Expose Grafana on NodePort `32000`
* Verify Grafana login page accessibility

---

# Infrastructure Details

| Server    | Hostname  | User |
| --------- | --------- | ---- |
| Jump Host | jump-host | thor |

---

# Requirements

Deployment:

```text id="1"
Name: grafana-deployment-datacenter
Image: grafana/grafana
```

Service:

```text id="2"
Type: NodePort
NodePort: 32000
```

Expected Result:

```text id="3"
Grafana Login Page Accessible
```

---

# Understanding the Problem

Without Monitoring:

```text id="4"
Applications Running
        │
        ▼
No Visibility
        │
        ▼
Difficult Troubleshooting
```

With Grafana:

```text id="5"
Applications
      │
      ▼
Metrics
      │
      ▼
Grafana Dashboards
      │
      ▼
Easy Monitoring
```

---

# What is Grafana?

Grafana is an open-source visualization and monitoring platform.

It helps teams:

* Monitor applications
* Visualize metrics
* Build dashboards
* Analyze logs
* Detect incidents

---

# Why DevOps Teams Use Grafana

Benefits:

* Real-time monitoring
* Infrastructure visibility
* Alerting
* Dashboard creation
* Performance analysis

---

# Real-World Monitoring Stack

```text id="6"
Application
      │
      ▼
Prometheus
      │
      ▼
Grafana
      │
      ▼
Dashboards
```

Grafana is often paired with Prometheus.

---

# Kubernetes Architecture

```text id="7"
           NodePort Service
                32000
                   │
                   ▼

     grafana-deployment-datacenter
                   │
                   ▼

             Grafana Pod
```

---

# Step-by-Step Implementation

## Step 1 – Connect to Jump Host

```bash id="8"
ssh thor@jump-host
```

### Explanation

Connects to the Kubernetes management node.

---

# Step 2 – Create Deployment YAML

```bash id="9"
vi grafana-deployment.yaml
```

### Explanation

Creates Kubernetes Deployment manifest.

---

# Step 3 – Add Deployment Configuration

```yaml id="10"
apiVersion: apps/v1
kind: Deployment

metadata:
  name: grafana-deployment-datacenter

spec:
  replicas: 1

  selector:
    matchLabels:
      app: grafana

  template:
    metadata:
      labels:
        app: grafana

    spec:
      containers:
      - name: grafana-container
        image: grafana/grafana
        ports:
        - containerPort: 3000
```

---

# YAML Breakdown

## Deployment

```yaml id="11"
kind: Deployment
```

Creates Deployment resource.

---

## Deployment Name

```yaml id="12"
name: grafana-deployment-datacenter
```

Task requirement.

---

## Replica Count

```yaml id="13"
replicas: 1
```

Deploys one Grafana Pod.

---

## Labels

```yaml id="14"
app: grafana
```

Used by Service selector.

---

## Container Name

```yaml id="15"
name: grafana-container
```

Container identifier.

---

## Grafana Image

```yaml id="16"
image: grafana/grafana
```

Official Grafana image.

---

## Container Port

```yaml id="17"
containerPort: 3000
```

Grafana listens on port 3000.

---

# Step 4 – Create Deployment

```bash id="18"
kubectl apply -f grafana-deployment.yaml
```

### Expected Output

```text id="19"
deployment.apps/grafana-deployment-datacenter created
```

---

# Step 5 – Verify Deployment

```bash id="20"
kubectl get deployments
```

Expected:

```text id="21"
grafana-deployment-datacenter
```

READY:

```text id="22"
1/1
```

---

# Step 6 – Verify Pod

```bash id="23"
kubectl get pods
```

Expected:

```text id="24"
grafana-deployment-datacenter-xxxxx
```

Status:

```text id="25"
Running
```

---

# Step 7 – Create Service YAML

```bash id="26"
vi grafana-service.yaml
```

---

# Step 8 – Add Service Configuration

```yaml id="27"
apiVersion: v1
kind: Service

metadata:
  name: grafana-service

spec:
  type: NodePort

  selector:
    app: grafana

  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 32000
```

---

# Service Breakdown

## Service Type

```yaml id="28"
type: NodePort
```

Exposes Grafana externally.

---

## Selector

```yaml id="29"
selector:
  app: grafana
```

Connects Service to Grafana Pods.

---

## Port Mapping

```yaml id="30"
port: 3000
targetPort: 3000
nodePort: 32000
```

Traffic Flow:

```text id="31"
32000
   │
   ▼
Service
   │
   ▼
3000
```

---

# Step 9 – Create Service

```bash id="32"
kubectl apply -f grafana-service.yaml
```

Expected:

```text id="33"
service/grafana-service created
```

---

# Step 10 – Verify Service

```bash id="34"
kubectl get svc
```

Expected:

```text id="35"
grafana-service
```

NodePort:

```text id="36"
32000
```

---

# Step 11 – Test Access

```bash id="37"
kubectl get svc grafana-service
```

Example:

```text id="38"
3000:32000/TCP
```

Access:

```text id="39"
http://<Node-IP>:32000
```

Expected:

```text id="40"
Grafana Login Page
```

---

# Kubernetes Networking Flow

```text id="41"
User
 │
 ▼
Node IP:32000
 │
 ▼
NodePort Service
 │
 ▼
Grafana Pod
 │
 ▼
Grafana UI
```

---

# Grafana Architecture

```text id="42"
Metrics Source
      │
      ▼
   Grafana
      │
      ▼
 Dashboards
      │
      ▼
 Monitoring
```

---

# Commands Used

```bash id="43"
ssh thor@jump-host

vi grafana-deployment.yaml

kubectl apply -f grafana-deployment.yaml

kubectl get deployments

kubectl get pods

vi grafana-service.yaml

kubectl apply -f grafana-service.yaml

kubectl get svc

kubectl get svc grafana-service
```

---

# Key Learnings

* Learned Grafana deployment on Kubernetes
* Created Kubernetes Deployments
* Created NodePort Services
* Exposed applications externally
* Understood observability tools
* Practiced monitoring platform deployment

---

# DevOps Concepts Covered

* Kubernetes
* Deployments
* Services
* NodePort
* Grafana
* Monitoring
* Observability

---

# Common Mistakes

## Mistake 1 – Wrong Deployment Name

Wrong:

```yaml id="44"
name: grafana
```

Correct:

```yaml id="45"
name: grafana-deployment-datacenter
```

---

## Mistake 2 – Wrong NodePort

Wrong:

```yaml id="46"
nodePort: 30000
```

Correct:

```yaml id="47"
nodePort: 32000
```

---

## Mistake 3 – Wrong Service Selector

Wrong:

```yaml id="48"
app: monitoring
```

Correct:

```yaml id="49"
app: grafana
```

Must match Pod labels.

---

## Mistake 4 – Wrong Container Port

Wrong:

```yaml id="50"
containerPort: 80
```

Correct:

```yaml id="51"
containerPort: 3000
```

Grafana listens on 3000.

---

# Interview Questions

## 1. What is Grafana?

An open-source platform used for monitoring, visualization, and dashboard creation.

---

## 2. Which port does Grafana use by default?

```text id="52"
3000
```

---

## 3. What is a NodePort Service?

A Kubernetes Service type that exposes applications externally via a node port.

---

## 4. How do you list Services?

```bash id="53"
kubectl get svc
```

---

## 5. Why is Grafana important in DevOps?

It provides visibility into infrastructure and application performance.

---

# Real DevOps Insight

Modern DevOps environments heavily rely on observability.

Typical Monitoring Stack:

```text id="54"
Applications
      │
      ▼
Prometheus
      │
      ▼
Grafana
      │
      ▼
Dashboards
      │
      ▼
Alerts
```

Grafana is used by organizations worldwide to monitor:

* Kubernetes Clusters
* Servers
* Databases
* Cloud Infrastructure
* Applications

Understanding Grafana is an important step toward mastering monitoring and observability.

---

# Conclusion

This task introduced Grafana deployment on Kubernetes. By creating a Deployment and exposing it through a NodePort Service, we learned how monitoring platforms are deployed and accessed in cloud-native environments. Grafana is a key tool in modern DevOps practices and forms a core component of observability stacks.

---
