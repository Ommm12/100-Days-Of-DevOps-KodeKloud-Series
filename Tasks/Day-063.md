# Day 63 – Deploy Iron Gallery App on Kubernetes

---

# Task Overview

The Nautilus DevOps team needed to deploy a customized Iron Gallery application along with its database backend on a Kubernetes cluster.

The deployment required creating a dedicated namespace, deploying both frontend and database applications, configuring persistent storage using volumes, defining resource limits, and exposing services for communication.

The objective was to:

* Create a namespace named `iron-namespace-nautilus`
* Deploy the Iron Gallery application
* Deploy the IronDB database
* Configure container resource limits
* Create required volume mounts
* Expose the database using a ClusterIP Service
* Expose the gallery application using a NodePort Service

---

# Infrastructure Details

| Server    | Hostname  | User |
| --------- | --------- | ---- |
| Jump Host | jump-host | thor |

---

# Requirements

Namespace:

```text
iron-namespace-nautilus
```

Frontend Deployment:

```text
Name: iron-gallery-deployment-nautilus
Image: kodekloud/irongallery:2.0
Replicas: 1
Label: run=iron-gallery
```

Database Deployment:

```text
Name: iron-db-deployment-nautilus
Image: kodekloud/irondb:2.0
Replicas: 1
Label: db=mariadb
```

Database Service:

```text
Name: iron-db-service-nautilus
Type: ClusterIP
Port: 3306
```

Frontend Service:

```text
Name: iron-gallery-service-nautilus
Type: NodePort
Port: 80
NodePort: 32678
```

---

# Understanding the Problem

Without Kubernetes:

```text
Frontend
      │
      ▼
Manual Configuration
      │
      ▼
Complex Management
```

With Kubernetes:

```text
Frontend Deployment
        │
        ▼
Service
        │
        ▼
Users

Database Deployment
        │
        ▼
ClusterIP Service
```

---

# Application Architecture

```text
                    Users
                      │
                      ▼
         NodePort Service (32678)
                      │
                      ▼
       Iron Gallery Deployment
                      │
                      ▼
                EmptyDir Volumes


        ClusterIP Service (3306)
                      │
                      ▼
          Iron DB Deployment
                      │
                      ▼
                EmptyDir Volume
```

---

# What is a Namespace?

Namespaces provide logical isolation inside a Kubernetes cluster.

Benefits:

* Resource organization
* Environment separation
* Access control
* Multi-team management

Example:

```text
Production Namespace
Development Namespace
Testing Namespace
```

---

# Step-by-Step Implementation

## Step 1 – Connect to Jump Host

```bash
ssh thor@jump-host
```

### Explanation

Connects to the Kubernetes administration node.

---

# Step 2 – Create Namespace

```bash
kubectl create namespace iron-namespace-nautilus
```

Expected:

```text
namespace/iron-namespace-nautilus created
```

---

# Step 3 – Create Manifest File

```bash
vi iron-gallery.yaml
```

---

# Step 4 – Add Kubernetes Configuration

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: iron-namespace-nautilus

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iron-gallery-deployment-nautilus
  namespace: iron-namespace-nautilus

spec:
  replicas: 1

  selector:
    matchLabels:
      run: iron-gallery

  template:
    metadata:
      labels:
        run: iron-gallery

    spec:
      containers:
      - name: iron-gallery-container-nautilus
        image: kodekloud/irongallery:2.0

        resources:
          limits:
            memory: "100Mi"
            cpu: "50m"

        volumeMounts:
        - name: config
          mountPath: /usr/share/nginx/html/data

        - name: images
          mountPath: /usr/share/nginx/html/uploads

      volumes:
      - name: config
        emptyDir: {}

      - name: images
        emptyDir: {}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iron-db-deployment-nautilus
  namespace: iron-namespace-nautilus

spec:
  replicas: 1

  selector:
    matchLabels:
      db: mariadb

  template:
    metadata:
      labels:
        db: mariadb

    spec:
      containers:
      - name: iron-db-container-nautilus
        image: kodekloud/irondb:2.0

        env:
        - name: MYSQL_DATABASE
          value: database_blog

        - name: MYSQL_ROOT_PASSWORD
          value: RootPass@123

        - name: MYSQL_PASSWORD
          value: UserPass@123

        - name: MYSQL_USER
          value: bloguser

        volumeMounts:
        - name: db
          mountPath: /var/lib/mysql

      volumes:
      - name: db
        emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: iron-db-service-nautilus
  namespace: iron-namespace-nautilus

spec:
  type: ClusterIP

  selector:
    db: mariadb

  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306

---
apiVersion: v1
kind: Service
metadata:
  name: iron-gallery-service-nautilus
  namespace: iron-namespace-nautilus

spec:
  type: NodePort

  selector:
    run: iron-gallery

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 32678
```

---

# Frontend Deployment Breakdown

## Image

```yaml
image: kodekloud/irongallery:2.0
```

Deploys the Iron Gallery application.

---

## Resource Limits

```yaml
limits:
  memory: 100Mi
  cpu: 50m
```

Restricts container resource usage.

---

## Volume Mounts

```yaml
config
```

Mounted at:

```text
/usr/share/nginx/html/data
```

---

```yaml
images
```

Mounted at:

```text
/usr/share/nginx/html/uploads
```

---

# Database Deployment Breakdown

## Image

```yaml
image: kodekloud/irondb:2.0
```

Deploys MariaDB backend.

---

## Environment Variables

```yaml
MYSQL_DATABASE
MYSQL_ROOT_PASSWORD
MYSQL_PASSWORD
MYSQL_USER
```

Used to initialize the database.

---

## Database Storage

```yaml
emptyDir: {}
```

Provides temporary storage for database files.

---

# Step 5 – Deploy Resources

```bash
kubectl apply -f iron-gallery.yaml
```

Expected:

```text
deployment.apps/iron-gallery-deployment-nautilus created
deployment.apps/iron-db-deployment-nautilus created
service/iron-gallery-service-nautilus created
service/iron-db-service-nautilus created
```

---

# Step 6 – Verify Deployments

```bash
kubectl get deploy -n iron-namespace-nautilus
```

Expected:

```text
iron-gallery-deployment-nautilus
iron-db-deployment-nautilus
```

READY:

```text
1/1
```

---

# Step 7 – Verify Pods

```bash
kubectl get pods -n iron-namespace-nautilus
```

Expected:

```text
Running
```

for both Pods.

---

# Step 8 – Verify Services

```bash
kubectl get svc -n iron-namespace-nautilus
```

Expected:

```text
iron-gallery-service-nautilus
iron-db-service-nautilus
```

---

# Service Architecture

```text
Users
  │
  ▼
NodePort 32678
  │
  ▼
iron-gallery-service-nautilus
  │
  ▼
Iron Gallery Pod


Iron Gallery Pod
      │
      ▼
iron-db-service-nautilus
      │
      ▼
Iron DB Pod
```

---

# Commands Used

```bash
ssh thor@jump-host

kubectl create namespace iron-namespace-nautilus

vi iron-gallery.yaml

kubectl apply -f iron-gallery.yaml

kubectl get deploy -n iron-namespace-nautilus

kubectl get pods -n iron-namespace-nautilus

kubectl get svc -n iron-namespace-nautilus
```

---

# Key Learnings

* Created Kubernetes Namespaces
* Deployed multi-tier applications
* Configured Deployments and Services
* Applied Resource Limits
* Used Environment Variables
* Mounted Volumes using emptyDir
* Exposed applications with NodePort and ClusterIP Services

---

# DevOps Concepts Covered

* Kubernetes Namespaces
* Deployments
* Services
* NodePort
* ClusterIP
* Resource Management
* Environment Variables
* Container Storage
* Multi-Tier Architecture

---

# Common Mistakes

## Mistake 1 – Wrong Namespace

Wrong:

```bash
kubectl get pods
```

Correct:

```bash
kubectl get pods -n iron-namespace-nautilus
```

---

## Mistake 2 – Incorrect Labels

Wrong:

```yaml
app: iron-gallery
```

Expected:

```yaml
run: iron-gallery
```

Labels must exactly match selectors.

---

## Mistake 3 – Missing Resource Limits

Without:

```yaml
resources:
```

Task validation fails.

---

## Mistake 4 – Wrong NodePort

Wrong:

```yaml
30080
```

Correct:

```yaml
32678
```

---

# Interview Questions

## 1. What is a Namespace in Kubernetes?

A logical partition used to isolate resources within a cluster.

---

## 2. What is the difference between ClusterIP and NodePort?

| ClusterIP            | NodePort            |
| -------------------- | ------------------- |
| Internal Access      | External Access     |
| Default Service Type | Opens Port On Nodes |

---

## 3. Why use Resource Limits?

Prevents a container from consuming excessive CPU and Memory.

---

## 4. What is emptyDir?

Temporary storage that exists as long as the Pod is running.

---

## 5. How do you verify Services?

```bash
kubectl get svc -n iron-namespace-nautilus
```

---

# Real DevOps Insight

This architecture reflects a common production pattern:

```text
Frontend Application
         │
         ▼
Internal Database
```

Managed through:

```text
Deployments
Services
Namespaces
Resource Limits
Volumes
```

These components form the foundation of modern Kubernetes-based applications.

---

# Conclusion

This task demonstrated how to deploy a complete multi-tier application on Kubernetes using Deployments, Services, Namespaces, Volumes, Resource Limits, and Environment Variables. These concepts are essential for deploying and managing production-ready applications in cloud-native environments.

---
