# Day 67 – Deploy Guest Book App on Kubernetes

---

# Task Overview

The Nautilus Application Development team completed development of a Guest Book application and requested deployment on the Kubernetes cluster.

The application follows a three-tier architecture:

* Redis Master (Backend)
* Redis Slave (Backend)
* Frontend Application

The objective was to:

* Deploy Redis Master
* Deploy Redis Slave
* Deploy Frontend Application
* Expose Frontend using NodePort Service
* Verify Guest Book Application accessibility

---

# Infrastructure Details

| Server    | Hostname  | User |
| --------- | --------- | ---- |
| Jump Host | jump-host | thor |

---

# Requirements

## Redis Master

```text
Deployment: redis-master
Replicas: 1
Container: master-redis-xfusion
Image: redis
CPU: 100m
Memory: 100Mi
Port: 6379
```

Service:

```text
Name: redis-master
Port: 6379
TargetPort: 6379
```

---

## Redis Slave

```text
Deployment: redis-slave
Replicas: 2
Container: slave-redis-xfusion
Image: gcr.io/google_samples/gb-redisslave:v3
CPU: 100m
Memory: 100Mi
Port: 6379
Environment:
GET_HOSTS_FROM=dns
```

Service:

```text
Name: redis-slave
Port: 6379
TargetPort: 6379
```

---

## Frontend

```text
Deployment: frontend
Replicas: 3
Container: php-redis-xfusion

Image:
gcr.io/google-samples/gb-frontend@sha256:a908df8486ff66f2c4daa0d3d8a2fa09846a1fc8efd65649c0109695c7c5cbff

CPU: 100m
Memory: 100Mi

Environment:
GET_HOSTS_FROM=dns

Port: 80
```

Service:

```text
Name: frontend
Type: NodePort
Port: 80
NodePort: 30009
```

---

# Architecture

```text
                 Users
                   │
                   ▼
         Frontend Service
          NodePort 30009
                   │
                   ▼
            Frontend Pods
                   │
                   ▼
           Redis Slave Pods
                   │
                   ▼
           Redis Master Pod
```

---

# Step 1 – Connect to Jump Host

```bash
ssh thor@jump-host
```

---

# Step 2 – Create Manifest File

```bash
vi guestbook.yaml
```

---

# Step 3 – Add Kubernetes Configuration

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-master
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-master
  template:
    metadata:
      labels:
        app: redis-master
    spec:
      containers:
      - name: master-redis-xfusion
        image: redis
        resources:
          requests:
            cpu: "100m"
            memory: "100Mi"
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-master
spec:
  selector:
    app: redis-master
  ports:
  - port: 6379
    targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-slave
spec:
  replicas: 2
  selector:
    matchLabels:
      app: redis-slave
  template:
    metadata:
      labels:
        app: redis-slave
    spec:
      containers:
      - name: slave-redis-xfusion
        image: gcr.io/google_samples/gb-redisslave:v3
        env:
        - name: GET_HOSTS_FROM
          value: dns
        resources:
          requests:
            cpu: "100m"
            memory: "100Mi"
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-slave
spec:
  selector:
    app: redis-slave
  ports:
  - port: 6379
    targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: php-redis-xfusion
        image: gcr.io/google-samples/gb-frontend@sha256:a908df8486ff66f2c4daa0d3d8a2fa09846a1fc8efd65649c0109695c7c5cbff
        env:
        - name: GET_HOSTS_FROM
          value: dns
        resources:
          requests:
            cpu: "100m"
            memory: "100Mi"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30009
```

---

# Step 4 – Deploy Application

```bash
kubectl apply -f guestbook.yaml
```

---

# Step 5 – Verify Deployments

```bash
kubectl get deploy
```

Expected:

```text
redis-master
redis-slave
frontend
```

---

# Step 6 – Verify Pods

```bash
kubectl get pods
```

Expected:

```text
1 Redis Master Pod
2 Redis Slave Pods
3 Frontend Pods
```

Total:

```text
6 Pods Running
```

---

# Step 7 – Verify Services

```bash
kubectl get svc
```

Expected:

```text
redis-master
redis-slave
frontend
```

NodePort:

```text
30009
```

---

# Commands Used

```bash
ssh thor@jump-host

vi guestbook.yaml

kubectl apply -f guestbook.yaml

kubectl get deploy

kubectl get pods

kubectl get svc
```

---

# Key Learnings

* Deployed Multi-Tier Applications
* Configured Redis Master-Slave Architecture
* Used Environment Variables
* Configured Resource Requests
* Exposed Applications with NodePort
* Managed Multiple Deployments and Services

---

# DevOps Concepts Covered

* Kubernetes Deployments
* Kubernetes Services
* NodePort
* Resource Requests
* Redis Architecture
* Three-Tier Applications
* Container Networking

---

# Interview Questions

## Why use Redis Master and Redis Slave?

Master handles writes while slaves handle read operations, improving scalability and performance.

---

## Why use NodePort?

Allows external access to applications running inside Kubernetes.

---

## What is the purpose of GET_HOSTS_FROM=dns?

Allows containers to discover services using Kubernetes DNS.

---

# Conclusion

This task demonstrated deployment of a complete Guest Book application using a multi-tier Kubernetes architecture consisting of Redis Master, Redis Slave, and Frontend components. It provided hands-on experience with Deployments, Services, Resource Management, and Kubernetes Networking.
